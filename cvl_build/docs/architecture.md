# Architecture

GitHub Pages = dashboard. GitHub Actions = compute/orchestration. Python = deterministic analysis/editing. KiCad = authoritative DRC. FreeRouting = autorouting. JSON = machine-readable reports.